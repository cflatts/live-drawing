let Router = require('express').Router
const apiRouter = Router()

let Dot = require('../data/schema.js').Dot

apiRouter.post('/drawing', function(request, response) {
    response.send()
})

apiRouter.get('/drawing', function(request, response) {
    response.send()
})